import React from 'react'
import './ButtonAddDocument.css'

const ButtonAddDocument = ({ id, onClick }) =>
  <button
    type="button"
    className="button-add-document"
    onClick={onClick}
  >
  </button>

export default ButtonAddDocument
